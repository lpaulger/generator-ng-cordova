language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-lp-cordova' ]; then cd .. && eval "mv $currentfolder generator-lp-cordova" && cd generator-lp-cordova; fi

